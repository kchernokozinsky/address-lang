SP a {a, b, c, d} k

!

f ... null => a; null => c;
a => c
return

# если перейти с помощью Формулы Вхождения, то будут инициализироваться переменные во внутреннем скоупе 
#  и ключевое слово return будет завершать роботу внутреннего скоупа и переходить к строке с указзаной меткой

# в случае вызова метки с помощью безусловного перехода, 
#  то алгоритм будет построчно выполнять инструкции до ключевого слова return, после чего закончит исполения программы
