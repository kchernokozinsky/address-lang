# inizialization of list with head 'h'
h = ["a", "b", "c", "d"];

i = 100; cur = h
# iterate over elements and print its values
L { 0, 1, 'cur != null => i} end end
    
    P { 'i == 0} | cur = 'cur
    
    Print { "element ", 'i, ": ", '(cur + 1) } 

end ...