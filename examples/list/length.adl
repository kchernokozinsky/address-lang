l = ["a", "b", "c", "d", "e", "f", "g"];

SP len {l, length}

Print { "Length of list is ", 'length}
!

# get length of the list
len ... null => list; null => len

    L {0, 1, true => i } l1
        
    P {D {list, 'i} == null} | @k
            'len = 'i; 
            return
        k...
    l1 ... 
 return   
 